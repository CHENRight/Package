CREATE TABLE `station_package`(
    `id` int
    `Order_Id` varchar (255) primary key,
    `owner_name` varchar (255),
    `phone_number` varchar (255),
    `status` int ,
    `appointment_time` varchar (255),
    `is_got` int
)